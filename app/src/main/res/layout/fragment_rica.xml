<?xml version="1.0" encoding="utf-8"?>
<ViewAnimator xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <FrameLayout
        android:id="@+id/preloadState"
        android:layout_width="match_parent"
        android:background="@drawable/card_grey"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/preloadTitleTv"
            style="@style/TextTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="@dimen/margin_top_home_title"
            android:textColor="@color/mid_gray"
            tools:text="Rica agent verification" />

    </FrameLayout>

    <FrameLayout
        android:background="@drawable/card_blue_outline"
        android:id="@+id/loadedState"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    </FrameLayout>

    <LinearLayout
        android:id="@+id/doneState"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/card_blue"
        android:orientation="vertical">

        <TextView
            android:id="@+id/doneTitleTv"
            style="@style/TextTitle"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="@dimen/margin_top_home_title"
            android:textColor="@android:color/white"
            tools:text="ID Verified" />

    </LinearLayout>
</ViewAnimator>